stages:
  - triggers

trigger_frontend:
  stage: triggers
  trigger:
    include: frontend/.gitlab-ci.yml
    strategy: depend
  rules:
    - changes:
        - frontend/*
trigger_code_runner:
  stage: triggers
  trigger:
    include: code-runner/.gitlab-ci.yml
    strategy: depend
  rules:
    - changes:
        - code-runner/*
