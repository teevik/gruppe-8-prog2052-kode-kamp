stages:
  - triggers

trigger_frontend:
  stage: triggers
  trigger:
    include: frontend/.gitlab-ci.yml
  rules:
    - changes:
        - frontend/*
# trigger_b:
#   stage: triggers
#   trigger:
#     include: b/.gitlab-ci.yml
#   rules:
#     - changes:
#         - b/*
